#include "BitReader.h"
#include "BitWriter.h"

#include <cassert>
#include <sstream>
#include <string>

int main() {
    std::stringstream output;

    // Test writing and reading a sequence that is not byte-aligned.
    {
        std::ofstream* unused = nullptr;
        (void)unused;
    }

    return 0;
}
